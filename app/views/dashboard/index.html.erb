<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>
  <!-- Include Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-light bg-secondary"> <!-- Added class "bg-secondary" -->
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="#">Dashboard</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link text-white" href="#">Home</a> <!-- Added class "text-white" -->
          </li>
          <li class="nav-item">
            <a class="nav-link text-white" href="#">Logout</a> <!-- Added class "text-white" -->
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4 text-center"> <!-- Added class "text-center" -->
    <h1 class = "text-center mb-5">User List</h1>
    <table class="table">
      <thead>
        <tr>
          <th>Email</th>
          <th>Password</th>
          <th>Action</th> <!-- Added Action column -->
        </tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.email %></td>
            <td><%= user.password_digest %></td>
            <td>
              <button class="btn btn-danger">Delete</button> <!-- Added Delete button -->
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <!-- Include Bootstrap JS (if needed) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
